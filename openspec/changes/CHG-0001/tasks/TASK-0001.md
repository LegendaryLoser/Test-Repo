---
id: TASK-0001
type: test-red
status: pending
covers:
  - REQ-ARCH-0001
  - REQ-ARCH-0002
  - REQ-ARCH-0003
  - REQ-ARCH-0004
  - REQ-ARCH-0005
  - REQ-ARCH-0006
  - REQ-ARCH-0007
  - REQ-ARCH-0008
dependencies: []
---

# TASK-0001 — Scaffold Python tooling + RED stubs for PHASE-0 tests

## Intent

Land the minimum Python infrastructure required to run PHASE-0 verification
tests, plus a `tools/ci/tests/test_phase0.py` whose eight tests each begin
in a failing state. This satisfies P4 (red-first) for `REQ-ARCH-0001..0008`
in git history before `TASK-0002` turns them green.

## Definition of done

- [ ] `pyproject.toml` at repo root declares Python project + pytest + pyyaml
      dev dependencies. Pytest `testpaths` includes `tools`.
- [ ] `tools/ci/__init__.py` and `tools/ci/tests/__init__.py` exist.
- [ ] `tools/ci/tests/conftest.py` defines a `repo_root` fixture pointing at
      the repository root and a `read_text(path)` helper.
- [ ] `tools/ci/tests/test_phase0.py` declares one test function per
      `REQ-ARCH-NNNN`, each annotated with `@test-id TEST-ARCH-NNNN` and
      `@covers REQ-ARCH-NNNN`, and each body calls
      `pytest.fail("RED: ...")` so the run is mechanically red.
- [ ] Running `pytest tools/ci/tests/test_phase0.py` locally reports 8 failed
      tests.

## Notes

- The annotations are docstring lines, not decorators — `tools/trace`
  (PHASE-2) will parse them; for now they are documentary.
- Commit message:
  - `Task: TASK-0001`
  - `Requirements: REQ-ARCH-0001, ..., REQ-ARCH-0008`
  - `Tests-Status: red`
  - `Phase: PHASE-0`
- No CI gate enforces the trailer format yet; we comply manually until
  PHASE-2.
