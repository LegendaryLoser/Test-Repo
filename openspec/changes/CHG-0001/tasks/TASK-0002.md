---
id: TASK-0002
type: impl
status: pending
covers:
  - REQ-ARCH-0001
  - REQ-ARCH-0002
  - REQ-ARCH-0003
  - REQ-ARCH-0004
  - REQ-ARCH-0005
  - REQ-ARCH-0006
  - REQ-ARCH-0007
  - REQ-ARCH-0008
dependencies:
  - TASK-0001
---

# TASK-0002 — Implement PHASE-0 verification assertions

## Intent

Replace each `pytest.fail("RED: ...")` stub from `TASK-0001` with real
assertions that mechanically verify the corresponding `REQ-ARCH-NNNN`
clause as stated in
[`openspec/architecture/ARCHITECTURE.md` §10](../../../architecture/ARCHITECTURE.md).

## Definition of done

- [ ] `TEST-ARCH-0001` — asserts every directory listed in `ARCHITECTURE.md`
      §3 exists, and no extra top-level entry exists outside the allowlist
      (`README.md`, `CLAUDE.md`, `.claude/`, `.github/`, `bmad/`, `openspec/`,
      `tools/`, `packages/`, `projects/`, `.gitignore`, `pyproject.toml`).
- [ ] `TEST-ARCH-0002` — asserts `ARCHITECTURE.md`, `ADR-0001..0008`, and
      `PHASE-0..5` files all exist with their declared IDs in frontmatter.
- [ ] `TEST-ARCH-0003` — asserts each empty product template
      (`vision.md`, `briefs/_TEMPLATE.md`, `prd/_TEMPLATE.md`,
      `epics/_TEMPLATE.md`, `stories/_TEMPLATE.md`) exists; asserts no
      *non-template* file exists in `briefs/`, `prd/`, `epics/`, `stories/`.
- [ ] `TEST-ARCH-0004` — asserts `openspec/specs/INDEX.yaml` parses as YAML
      and has top-level keys `version`, `generated_at`, `requirements` with
      `requirements: []` (empty list permitted at PHASE-0).
- [ ] `TEST-ARCH-0005` — walks every `[text](path)` link in
      `openspec/architecture/` markdown; asserts every relative target
      resolves to an existing file.
- [ ] `TEST-ARCH-0006` — asserts no file under `openspec/architecture/` has
      a Python/TypeScript/JavaScript/Shell extension or shebang.
- [ ] `TEST-ARCH-0007` — extracts the five principles from
      `ARCHITECTURE.md` §1 and the five principles from `CLAUDE.md`; asserts
      they are byte-equal in order and content.
- [ ] `TEST-ARCH-0008` — loads `.claude/settings.json` as JSON; asserts the
      `hooks` object declares all five events specified by
      [`ADR-0005` §6](../../../architecture/decisions/ADR-0005-traceability-and-journaling.md)
      (`SessionStart`, `PreToolUse`, `PostToolUse`, `Stop`, `SessionEnd`).
- [ ] Running `pytest tools/ci/tests/test_phase0.py` locally reports 8 passed.

## Notes

- Commit message:
  - `Task: TASK-0002`
  - `Requirements: REQ-ARCH-0001, ..., REQ-ARCH-0008`
  - `Tests-Status: red→green`
  - `Phase: PHASE-0`
- After this commit, PHASE-0 exit criteria are met. PHASE-1 unblocks.
