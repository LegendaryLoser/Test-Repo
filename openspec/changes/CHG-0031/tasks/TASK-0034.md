---
id: TASK-0034
type: docs
status: pending
covers:
  - REQ-SPEC-0016
dependencies:
  - TASK-0033
---

# TASK-0034 — GREEN: write STATUS.md + update CLAUDE.md + add REQ-SPEC-0016

## Intent

Replace TASK-0033's RED state by:
1. Writing `openspec/STATUS.md` with all required sections.
2. Updating `CLAUDE.md` "How to start any session" to point at
   STATUS.md as the first artifact to read.
3. Adding `REQ-SPEC-0016` to `openspec/specs/_meta/spec-storage.spec.md`.
4. Regenerating INDEX.yaml (16 entries).

## Definition of done

- [ ] `openspec/STATUS.md` exists with the 5 required sections.
- [ ] STATUS.md tables list every CHG (0001..0031 visible).
- [ ] STATUS.md audit-findings ledger lists every finding from the
      in-session audit with status and resolving CHG.
- [ ] STATUS.md "Next session: start here" section gives unambiguous
      pointer to the next action (spawn `bmad-review-adversarial-general`
      per user request).
- [ ] `CLAUDE.md` "How to start any session" addendum.
- [ ] `REQ-SPEC-0016` added with atomic acceptance.
- [ ] `python -m tools.spec_lint index` regenerates INDEX.yaml (now
      16 entries).
- [ ] `python -m tools.spec_lint validate openspec` → exit 0.
- [ ] `python -m tools.spec_lint check-layout` → exit 0.
- [ ] `python -m tools.spec_lint index --check` → exit 0.
- [ ] `pytest tools/` → all-green; TASK-0033's RED test passes.
- [ ] Commit trailer `Tests-Status: red→green`.
