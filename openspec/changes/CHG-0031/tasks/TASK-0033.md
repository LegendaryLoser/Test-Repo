---
id: TASK-0033
type: test
status: pending
covers:
  - REQ-SPEC-0016
dependencies: []
---

# TASK-0033 — RED meta-test for STATUS.md presence + sections

## Intent

Ship the meta-test asserting `openspec/STATUS.md` exists with the
required sections, RED first per P4. TASK-0034 GREEN writes the file.

## Definition of done

- [ ] `tools/ci/tests/test_status_resume.py` exists with a single
      test `test_status_resume_file_has_required_sections`.
- [ ] Test asserts `openspec/STATUS.md` exists.
- [ ] Test asserts the file contains all required section headers
      (each as an exact substring match on a line):
  - `## CHG status`
  - `## Audit findings`
  - `## Open architectural questions`
  - `## Roadmap`
  - `## Next session: start here`
- [ ] Annotations: `@test-id TEST-SPEC-0105`, `@covers REQ-SPEC-0016`
      (forward-referenced; REQ lands in TASK-0034).
- [ ] Full suite: only the new test fails (assertion-red, not
      collection-red); no regression elsewhere.
- [ ] Commit trailer `Tests-Status: red`.
