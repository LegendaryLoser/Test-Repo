---
id: TASK-0043
type: docs
status: done
covers:
  - REQ-AUDIT-0001
dependencies:
  - TASK-0042
---

# TASK-0043 — META- audit corrections log + findings-index Wave 4 + Wave 5 catch-up

## Intent

The Wave-5 `META-` stream (TASK-0042) produced 19 corrections against the existing audit corpus: 4 CRIT (THEME-P mis-attribution, findings-index.md Wave-4 omission, FIRST-CRIT-01 method artifact, STAKE/COUNTER raw-count inflation), 12 SER (theme over-generalisations, σ × κ scoring errors, cell mis-assignments, severity mis-calibrations), 3 PROC (ACGR denominator precision, anonymous citation hazard, σ precision exceeding documented uncertainty).

Per STATUS.md "Next session: start here" sequencing, these corrections must land BEFORE any further consolidation (Wave-5 thematic clustering, qd-triage.md §3.5 σ × κ for the 4 new streams, ACGR re-measurement) — otherwise downstream work compounds errors.

This TASK applies the corrections via an append-only audit-of-the-audit log (`corrections.md`) plus targeted inline annotations, AND brings `findings-index.md` up to date with Waves 4 and 5 (the index was stale at Wave 3 — META-CRIT-002 surfaced this as a structural defect).

## Definition of done

- [x] `openspec/_bmad-output/knowledge/audit/2026-05-17-architecture/corrections.md` authored — 19 META corrections logged with status field (applied / annotate-only / deferred-to-ADR). Authoritative for corrected attributions and counts pending the qd-triage §3.5 re-issue.
- [x] `consolidated.md` header updated:
      - Total streams reflects 40 (Waves 1-5).
      - Total raw findings reflects ~770.
      - Convergence framing notes COMPOSITE-V2 Gate 6 → ACGR transition.
      - Pointer to `corrections.md` added.
- [x] `consolidated.md` THEME-X updated with STAKE-PROC-020 as second surfacer (META-SER-003 applied inline; "Streams: 1/12" corrected to "Streams: 2/40 · Constituent findings: 2").
- [x] `qd-triage.md` companion-documents block updated with pointer to `corrections.md` and a summary of the most critical defects (PROSE-THEME-P, STAKE raw count, PAIGE κ count, cell over-saturation count).
- [x] `findings-index.md` updated:
      - Header restated for 40 streams across 5 waves; per-theme constituent listing for Waves 4-5 deferred to next consolidation (cross-referenced to corrections.md META-CRIT-002).
      - Wave 4 per-stream tally section added (8 streams: RED2, RETRO2, EDGE2, INHER2, VALID2, PREM2, STAKE, COUNTER).
      - Wave 5 per-stream tally section added with cell assignments (GOV, SEC, GOVDEV, META).
      - STAKE row notes the raw-count correction (20 not ~25) per META-CRIT-004.
      - FIRST row annotated with the FIRST-CRIT-01 retraction per META-CRIT-003.
      - Grand total reflects 40 streams + ~770 raw findings + 19 META corrections.
- [x] `README.md` table updated to list `corrections.md` and to note qd-triage §3.5 is pending.
- [x] `python -m tools.spec_lint validate openspec` → exit 0.
- [x] `python -m tools.spec_lint index --check` → exit 0.
- [x] `pytest tools/ci/tests/test_status_resume.py` → pass.
- [x] Commit trailer `Tests-Status: none`.

## Out of scope (deferred to TASK-0044)

- **Wave-5 thematic consolidation in consolidated.md.** Adding new themes for Wave-5 findings (likely from `SEC-` security-architecture themes and `META-` corpus-quality themes; `GOV-` and `GOVDEV-` largely reinforce existing governance themes per cell pairing); per-theme sole-source attribution; "Wave 5 supplement" section after the existing "Wave 4 supplement."
- **qd-triage.md §3.5.** Per-stream σ × κ scoring for GOV, SEC, GOVDEV, META. Folds in the corrections from this TASK (PROSE PAIGE STAKE COUNTER κ adjustments; STAKE cell-occupancy refinement per META-SER-004).
- **qd-triage.md §4 cell maps + §7 ACGR re-measurement.** Recompute the (cell, theme) archive after Wave 5; report ACGR with uncertainty bands per META-PROC-001.
- **Per-theme constituent-finding listing in findings-index.md for Waves 4-5.** Requires the thematic consolidation above.
- **Wave 6 decision.** Conditional on the ACGR re-measurement output.

## Notes

- **Correction philosophy.** Rather than silently overwriting the original framings in `consolidated.md` and `qd-triage.md`, this TASK preserves them and adds an audit-of-the-audit log. This models the correction protocol the methodology codification ADR will need to specify, and respects the audit's own append-only discipline. Surgical inline edits are limited to: (a) corrections whose original framing is mechanically wrong AND fixable in one line (THEME-X count); (b) marker annotations at the top of affected documents pointing readers at `corrections.md`.
- **3 PROC-class corrections are deferred to the methodology codification ADR** (queued; no ID allocated yet — next available is ADR-0009 unless taken first). <!-- spec-lint: allow prose-xref-banned --> META-PROC-001/002/003 surface measurement-discipline gaps (ACGR denominator, citation rigor, σ precision) that need formal treatment in §6 of the methodology ADR, not inline annotations. <!-- spec-lint: allow prose-xref-banned --> They are recorded in `corrections.md` with `Status: defer` and will be addressed when the ADR is drafted.
- **Next available TASK number after this commit: TASK-0044** (Wave-5 thematic consolidation + qd-triage §3.5 + ACGR re-measurement).

## Risk

- **`corrections.md` is staging.** Like the rest of `_bmad-output/`, this log is non-authoritative until promoted. The corrections recorded here do not modify any spec file or any production code; they are descriptive analysis of the audit corpus.
- **The qd-triage.md row text was NOT updated inline.** A reader who consults qd-triage.md §3 directly will see the original (uncorrected) σ × κ rows; they must read `corrections.md` to find the authoritative re-statements. This is intentional (provenance preservation) but creates a temporary inconsistency until the §3.5 re-issue lands in TASK-0044.
- **FIRST stream Tier-A admission is unaffected by FIRST-CRIT-01 retraction.** FIRST's 4 sole themes (II, KK, LL, NN) do not depend on FIRST-CRIT-01. The retraction is local to a single artifact-class finding.
- **META- itself was not meta-audited.** The reasoning-tree meta-auditor is a Wave-5 stream and not subject to a meta-meta-audit. If META- itself contains misreadings, they propagate into this corrections log. The next consolidation pass should spot-check a sample of META-'s claims against the cited findings (e.g., re-verify THEME-P attribution by grepping PROSE-findings.md for "paper" / "transformer associative memory") — done for META-CRIT-001 in this TASK (PROSE-findings.md grep confirmed zero references to "the paper").
