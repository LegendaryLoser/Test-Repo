---
id: TASK-0023
type: impl
status: pending
covers:
  - REQ-ARCH-0001
  - REQ-SPEC-0001
dependencies:
  - TASK-0022
---

# TASK-0023 — GREEN: implement `top-level-allowlist` rule + `openspec-validate` CLI

## Intent

Replace the TASK-0022 skeletons with real implementations such that
every test from TASK-0022 turns green. Resolve the duplicated constants
between `test_phase0.py` and the new rule. Land the REQ that
`openspec-validate` covers.

## Definition of done

- [ ] `tools/spec_lint/rules/top_level_allowlist.py:TopLevelAllowlist.check_repo()`
      implemented: runs `git ls-files`, splits on `/`, derives the set
      of tracked top-level entries, returns one `Finding` per missing
      required entry and per unexpected unlisted entry. Severity:
      `error` for both. Pulls allowlist + required from
      `tools/spec_lint/_top_level_allowlist.py`.
- [ ] `tools/spec_lint/__main__.py` implemented:
  - `validate [path]` discovers every `*.spec.md` and `*.md` under
    the target path, parses spec files, runs every applicable rule
    (`Rule`, `CrossFileRule`, `CorpusRule`), collects Findings, prints
    them, exits 1 if non-empty.
  - `check-layout` instantiates `TopLevelAllowlist` and runs
    `check_repo(<cwd>)`, prints Findings, exits 1 if non-empty.
- [ ] `tools/ci/tests/test_phase0.py`:
  - Imports `REQUIRED_TOP_LEVEL_DIRS`, `ALLOWED_TOP_LEVEL_ENTRIES`
    from `tools.spec_lint._top_level_allowlist` (delete local copies).
  - `test_arch_0001_top_level_layout` becomes a 3-line wrapper that
    asserts `TopLevelAllowlist().check_repo(repo_root) == []`.
- [ ] `openspec/specs/_meta/spec-storage.spec.md` created with
      `REQ-SPEC-0001` covering `openspec-validate` (the aggregate
      "openspec/ format" assertion). Frontmatter includes `references`
      to `ADR-0004` and `ADR-0008`. Body is one assertion, atomic.
- [ ] `openspec/architecture/ARCHITECTURE.md` §3: add `.gitignore` and
      `pyproject.toml` lines to the top-level tree. Comment notes
      "tracked, enforced by `top-level-allowlist`".
- [ ] `python -m tools.spec_lint validate openspec/` exits `0` on the
      current tree.
- [ ] `python -m tools.spec_lint check-layout` exits `0` on the current
      tree.
- [ ] Full suite: `pytest tools/` → all-green (TASK-0022's tests
      now pass; nothing regressed).
- [ ] Commit trailer `Tests-Status: red→green`.

## Notes

- Keep the CLI under 80 lines. It's a thin orchestrator — argparse +
  rule discovery + Finding printing. Real logic lives in the rules.
- Rule discovery: iterate `pkgutil.iter_modules(rules.__path__)`, import
  each, find the class whose `id` matches a known prefix, instantiate.
  Or just hardcode the import list — the rule set changes ~once per
  CHG, hardcoding is fine and easier to read. Pick the simpler path.
- The skeleton in `__main__.py` should be replaced wholesale, not
  patched, so the diff cleanly shows "real implementation lands now".
