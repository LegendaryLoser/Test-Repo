---
id: TASK-0022
type: test
status: pending
covers:
  - REQ-ARCH-0001
  - REQ-SPEC-0001
dependencies: []
---

# TASK-0022 — RED: failing tests for `top-level-allowlist` + `openspec-validate`

## Intent

Stand up the test surface for both gates in CHG-0011 before any
implementation exists. Per P4 (red-first), the failing state must be in
git history before the GREEN commit.

## Definition of done

- [ ] `tools/spec_lint/rules/top_level_allowlist.py` — skeleton class
      `TopLevelAllowlist` with `id`, `description`, and a no-op
      `check_repo()` returning `[]`. Allows tests to import without
      `ImportError`.
- [ ] `tools/spec_lint/__main__.py` — skeleton CLI with `validate`
      and `check-layout` subcommands that currently exit `0` without
      doing anything. Allows subprocess tests to invoke without
      `ModuleNotFoundError`.
- [ ] `tools/spec_lint/rules/base.py` — `RepoRule` Protocol added
      alongside the existing four. No-op signature only; nothing
      depends on it yet.
- [ ] `tools/spec_lint/_top_level_allowlist.py` — new module with
      `REQUIRED_TOP_LEVEL_DIRS` and `ALLOWED_TOP_LEVEL_ENTRIES`
      constants. Identical values to `test_phase0.py`'s current
      definitions; not yet imported anywhere except the new tests.
- [ ] `tools/spec_lint/tests/test_top_level_allowlist.py` — at least
      four assertion-failing tests covering: clean repo passes; missing
      required dir surfaces a finding; unexpected entry surfaces a
      finding; the real repo passes when run via `check_repo(repo_root)`.
- [ ] `tools/spec_lint/tests/test_openspec_validate.py` — at least
      three subprocess-based assertion-failing tests covering: `validate
      openspec/` reports 0 findings on a clean tree; a planted
      ill-formed REQ block surfaces a finding with non-zero exit; the
      `check-layout` subcommand surfaces a finding for an injected
      unexpected entry under `tmp_path`.
- [ ] Running `pytest tools/spec_lint/tests/test_top_level_allowlist.py
      tools/spec_lint/tests/test_openspec_validate.py` shows **failures
      (red)**, not collection errors. Every other test stays green
      (no regression — full `pytest tools/` must show only the new
      tests failing).
- [ ] Commit trailer `Tests-Status: red`; commit body lists each new
      test ID and which clause of which REQ it covers.

## Notes

- Test IDs: allocate from `TEST-SPEC-NNNN` (continuing the existing
  series in `test_xref_resolves.py`'s neighborhood — check the highest
  TEST-SPEC-* allocated before picking).
- The skeleton files are necessary for tests to be **assertion-red**
  rather than **collection-red**. The skeletons return wrong answers
  (empty findings, exit 0); the GREEN commit replaces the implementation
  body without changing public signatures.
