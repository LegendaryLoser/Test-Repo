---
id: TASK-0012
type: test-red
status: pending
covers: []
dependencies:
  - TASK-0011
---

# TASK-0012 — Scaffold property tests (RED)

## Intent

Add `hypothesis` dev dep; create `tools/spec_lint/tests/properties/`
directory; add one test file per rule (or rule family) with `pytest.fail`
stubs. Tests fail. P4-compliant red history before TASK-0013.

## Definition of done

- [ ] `pyproject.toml` `[project.optional-dependencies].dev` includes
      `hypothesis>=6.100`.
- [ ] `tools/spec_lint/tests/properties/__init__.py` exists.
- [ ] Six test files exist under `tests/properties/` with `pytest.fail("RED")`
      stubs in every test:
      `test_parser_properties.py`,
      `test_req_id_format_properties.py`,
      `test_spec_frontmatter_valid_properties.py`,
      `test_compound_properties.py`,
      `test_anti_aliasing_properties.py`,
      `test_xref_properties.py`.
- [ ] Each stub references the property it will assert in a docstring so the
      green commit's diff is small.
- [ ] `pytest tools/spec_lint/tests/properties/` fails (every test
      `pytest.fail`).
- [ ] `pytest tools/` (full suite minus the new properties dir) still 65/65.

## Notes

- Commit trailer: `Task: TASK-0012`, `Requirements: -`, `Tests-Status: red`,
  `Phase: PHASE-1`.
- Push immediately after commit.
