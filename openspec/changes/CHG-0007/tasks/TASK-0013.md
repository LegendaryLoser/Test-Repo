---
id: TASK-0013
type: impl
status: pending
covers: []
dependencies:
  - TASK-0012
---

# TASK-0013 — Implement property bodies; fix any defects surfaced

## Intent

Replace the `pytest.fail` stubs from TASK-0012 with real `hypothesis`
properties. If any property fails against the current rule implementation,
treat the failure as a real defect and fix the rule. The fix is its own
sub-commit so the red-first sequence per-property is preserved in git
history.

## Definition of done

- [ ] Every property test from CHG-0007 proposal §"What changes" is
      implemented with a `hypothesis.strategies` strategy and an explicit
      assertion of the named invariant.
- [ ] `@settings(max_examples=50)` applied per property to keep CI fast.
- [ ] If any property fails on the existing rule, the rule is fixed and
      the fix carries a `Defect-Surfaced-By: TEST-SPEC-NNNN` trailer.
- [ ] `pytest tools/` passes end-to-end (existing 65 + the new properties).
- [ ] Total runtime stays under 5 seconds.

## Notes

- Commit trailer for property bodies:
  `Task: TASK-0013`, `Requirements: -`, `Tests-Status: red→green`,
  `Phase: PHASE-1`.
- Commit trailer for any defect fix:
  `Task: TASK-0013-fix-N`, `Defect-Surfaced-By: TEST-SPEC-NNNN`,
  `Tests-Status: red→green`, `Phase: PHASE-1`.
- Push after each commit.
