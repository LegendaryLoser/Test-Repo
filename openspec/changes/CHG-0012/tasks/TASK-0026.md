---
id: TASK-0026
type: impl
status: pending
covers:
  - REQ-SPEC-0002
  - REQ-SPEC-0003
dependencies:
  - TASK-0025
---

# TASK-0026 — GREEN: implement `bmad-direct-reference` + `mock-in-repo-banned`

## Intent

Replace TASK-0025 skeletons with real CrossFileRule implementations.
Add REQ-SPEC-0002 + REQ-SPEC-0003 to
`openspec/specs/_meta/spec-storage.spec.md`. Wire both rules into
`python -m tools.spec_lint validate`.

## Definition of done

- [ ] `bmad_direct_reference.py:BmadDirectReference.check_files()`
      implemented per proposal §1:
  - Python import + executable-path patterns.
  - TS/JS import patterns (no matches today but in place for PHASE-3+).
  - `tools/tea/` allowlist skip.
  - Inline `<!-- spec-lint: allow bmad-direct-reference -->` skip.
  - Rule's own file structurally exempt.
- [ ] `mock_in_repo_banned.py:MockInRepoBanned.check_files()`
      implemented per proposal §2:
  - Python banned-API patterns with string-target resolution (allow
    external prefixes; flag in-repo + unparseable).
  - JS/TS patterns wired but inert.
  - Inline allow marker support + structural exemption of rule's own
    file.
- [ ] `tools/spec_lint/__main__.py:_cmd_validate` adds both rules.
- [ ] `openspec/specs/_meta/spec-storage.spec.md` extended with
      `REQ-SPEC-0002` and `REQ-SPEC-0003`.
- [ ] `python -m tools.spec_lint validate openspec` → exit 0 (specs
      tree is unchanged; rules don't scan it for these new gates, but
      they're loaded by the CLI so smoke-runs successfully).
- [ ] A new CLI subcommand or extension to `validate` that also runs
      the CrossFileRules against `tools/`, `packages/`, `projects/` —
      either `validate-code [path]` or `validate` expanded to scan
      both. Decide in implementation, document choice in commit.
- [ ] `pytest tools/` → all-green; TASK-0025's tests now pass.
- [ ] Commit trailer `Tests-Status: red→green`.

## Notes

- The CLI needs a way to run rules against source-code dirs, not just
  `openspec/`. Cleanest: extend `_cmd_validate` to also scan
  `tools/`, `packages/`, `projects/` for CrossFileRules whose
  `applies_to_code = True`. Add a per-rule `applies_to` flag.
- Alternative: a `validate-code` subcommand parallel to `validate`.
  Less elegant but lower blast radius.
- Whichever path: the two new rules MUST be exercised by the CLI on
  the real repo, not only by unit tests. Otherwise the gate has
  unit-test coverage but no production execution.
