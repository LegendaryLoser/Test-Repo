---
id: TASK-0025
type: test
status: pending
covers:
  - REQ-SPEC-0002
  - REQ-SPEC-0003
dependencies: []
---

# TASK-0025 — RED tests for `bmad-direct-reference` + `mock-in-repo-banned`

## Intent

Stand up failing tests + no-op rule skeletons for both gates in
CHG-0012. Per P4, the failing state must precede the GREEN commit
in git history.

## Definition of done

- [ ] `tools/spec_lint/rules/bmad_direct_reference.py` — skeleton class
      `BmadDirectReference` (id, description, `check_files()` returning
      `[]`).
- [ ] `tools/spec_lint/rules/mock_in_repo_banned.py` — skeleton class
      `MockInRepoBanned` (id, description, `check_files()` returning
      `[]`).
- [ ] `tools/spec_lint/tests/test_bmad_direct_reference.py` — at least
      five assertion-failing tests covering: `from _bmad...` import
      flagged; `import _bmad...` flagged; `subprocess.run` with
      `_bmad/` path flagged; clean file passes; real repo passes.
- [ ] `tools/spec_lint/tests/test_mock_in_repo_banned.py` — at least
      six assertion-failing tests covering: `patch("tools.x")` flagged;
      `patch("requests.get")` allowed; `MagicMock()` flagged
      (conservative); `monkeypatch.setattr` flagged; clean file passes;
      real repo passes.
- [ ] `pytest tools/` shows the new tests failing (assertion-red,
      not collection-red) and every other test still passing.
- [ ] Commit trailer `Tests-Status: red`.

## Notes

- Test IDs continue from `TEST-SPEC-0085` (next free after CHG-0011).
- Skeletons return `[]` so golden-path tests (clean file, real repo)
  trivially pass; tests expecting specific findings assertion-fail
  cleanly.
- Allow-marker tests are deferred to the GREEN commit's
  implementation step — they require the rule to actually scan, and
  a no-op skeleton can't demonstrate them.
