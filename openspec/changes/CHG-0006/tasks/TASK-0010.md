---
id: TASK-0010
type: test-red
status: pending
covers: []
dependencies:
  - TASK-0009
---

# TASK-0010 — Scaffold compound + anti-aliasing stubs + RED tests

## Intent

Add stubs, config, protocol addition, and real test bodies. Stubs raise
`NotImplementedError`; tests fail at the call site.

## Definition of done

- [ ] `tools/spec_lint/_sections.py` exposes `extract_section(body, header)`.
- [ ] `tools/spec_lint/rules/base.py` extended with `CorpusRule` protocol.
- [ ] `tools/spec_lint/rules/compound_requirement_detector.py` exists;
      `check` raises NIE.
- [ ] `tools/spec_lint/rules/anti_aliasing.py` exists; `check_corpus` raises
      NIE.
- [ ] `tools/spec_lint/config.yaml` present with `anti_aliasing.ngram_size`,
      `ngram_type`, `threshold`.
- [ ] `tools/spec_lint/tests/test_compound_requirement_detector.py` covers
      single-G/W/T, two-G/W/T, zero-G/W/T, no-Acceptance-section.
- [ ] `tools/spec_lint/tests/test_anti_aliasing.py` covers identical bodies,
      near-duplicates, distinct bodies, single-REQ, custom-config threshold,
      char and word n-gram modes.
- [ ] `pytest tools/spec_lint/` shows failures for every new behavior test;
      attribute-check tests pass; prior 52 tests unchanged.
- [ ] `pytest tools/ci/` still 8/8.

## Notes

- Commit trailer: `Task: TASK-0010`, `Requirements: -`, `Tests-Status: red`,
  `Phase: PHASE-1`.
- Push immediately after commit.
