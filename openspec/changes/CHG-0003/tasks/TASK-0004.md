---
id: TASK-0004
type: test-red
status: pending
covers: []
dependencies: []
---

# TASK-0004 — Scaffold spec_lint + RED tests

## Intent

Land the `tools/spec_lint/` package skeleton with stub implementations that
raise `NotImplementedError`, plus full test bodies and fixtures. Tests fail
because the stubs raise NIE on every call. Establishes P4-compliant red
history before TASK-0005 supplies the real implementations.

## Definition of done

- [ ] `tools/__init__.py` (empty) so `tools` is an importable package.
- [ ] `pyproject.toml` `[tool.pytest.ini_options]` carries `pythonpath = ["."]`.
- [ ] Path-form `tools/spec-lint` references in `openspec/architecture/` and
      `.github/workflows/` renamed to `tools/spec_lint`. Prose / identifier
      references unchanged.
- [ ] `tools/spec_lint/{__init__,models,parser}.py` exist; `parser.parse_spec_file`
      raises `NotImplementedError("…RED stub…")`.
- [ ] `tools/spec_lint/rules/{__init__,base,req_id_format,spec_frontmatter_valid}.py`
      exist; each rule's `check()` raises `NotImplementedError`.
- [ ] `tools/spec_lint/tests/fixtures/valid/` contains at least
      `simple.spec.md` and `multi_req.spec.md`.
- [ ] `tools/spec_lint/tests/fixtures/invalid/req_id/` contains at least four
      distinct violation classes (lowercase domain, long domain, short number,
      non-digit in number).
- [ ] `tools/spec_lint/tests/fixtures/invalid/frontmatter/` contains at least
      five distinct violation classes (missing block, missing status,
      id mismatch, bad status, bad tier, bad revision, missing references key).
- [ ] `tools/spec_lint/tests/{test_parser,test_req_id_format,test_spec_frontmatter_valid}.py`
      exist with real test bodies (no `pytest.fail` stubs — failure comes
      from the stub modules raising NIE).
- [ ] Running `pytest tools/spec_lint/` reports failures for every spec_lint
      test (parser tests fail at `parse_spec_file`; rule tests fail at
      `parse_spec_file` or `.check()`).
- [ ] PHASE-0 tests still pass (`pytest tools/ci/`).

## Notes

- Commit trailer:
  - `Task: TASK-0004`
  - `Requirements: -` (no REQ-IDs yet; spec-storage REQs land in CHG-0007
    after all PHASE-1 rules are implemented).
  - `Tests-Status: red`
  - `Phase: PHASE-1`
- Push immediately after the commit per ADR-0005 §6 amendment 0001 (push
  invariant). Until PHASE-2 hooks land, the push is manual.
