---
id: TASK-0027
type: test
status: pending
covers:
  - REQ-SPEC-0013
dependencies: []
---

# TASK-0027 — RED meta-test for annotation discipline

## Intent

Ship the test that enforces [`CLAUDE.md`](../../../../CLAUDE.md) "How to
add a test" annotation requirements. RED first (P4): the test exists
in a failing state in this commit; the GREEN commit (TASK-0028)
backfills the 92 violations.

## Definition of done

- [ ] `tools/ci/tests/test_annotation_discipline.py` exists.
- [ ] Single test function `test_every_test_has_id_and_covers` walks
      every `test_*.py` under `tools/`, AST-parses each, enumerates
      every `def test_*` function, and asserts each docstring contains:
  - `@test-id TEST-<DOMAIN>-...` (regex anchored on `@test-id`
    followed by `TEST-` prefix)
  - `@covers REQ-<X>-<NNNN>` (regex; at least one REQ-ID, list form
    `REQ-A-0001, REQ-B-0002` accepted)
- [ ] The test asserts a structured report (sorted list of
      `file:line: function: missing-annotation`) for clear RED output.
- [ ] The test self-exempts (does not assert on itself, to avoid
      circular self-reference during initial development; will be
      tightened in TASK-0028 once it's compliant).
- [ ] `pytest tools/ci/tests/test_annotation_discipline.py` shows
      assertion-red (specific violations enumerated), NOT collection-red.
- [ ] `pytest tools/` shows the new test failing + all other tests
      still passing (no regression).
- [ ] Commit trailer `Tests-Status: red`.

## Notes

- Test ID: TEST-SPEC-0099 (next free after CHG-0012's TEST-SPEC-0098).
- The self-exemption is removed in TASK-0028 once its own docstring is
  compliant — at that point the test asserts on itself too.
- REQ-SPEC-0013 is referenced in the `@covers` annotation but is not yet
  defined as a spec file entry. It lands in TASK-0028. The forward
  reference is acceptable per the pattern established in CHG-0011
  (REQ-SPEC-0001 was forward-referenced in TASK-0022 RED).
