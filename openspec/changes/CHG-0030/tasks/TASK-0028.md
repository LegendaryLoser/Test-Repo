---
id: TASK-0028
type: docs
status: pending
covers:
  - REQ-SPEC-0004
  - REQ-SPEC-0005
  - REQ-SPEC-0006
  - REQ-SPEC-0007
  - REQ-SPEC-0008
  - REQ-SPEC-0009
  - REQ-SPEC-0010
  - REQ-SPEC-0011
  - REQ-SPEC-0012
  - REQ-SPEC-0013
dependencies:
  - TASK-0027
---

# TASK-0028 — GREEN: create 10 REQs + backfill 92 annotations

## Intent

Replace the RED state from TASK-0027 with full compliance. Create the
10 new REQ-SPEC entries the meta-test references, and backfill every
test annotation.

## Definition of done

- [ ] `openspec/specs/_meta/spec-storage.spec.md` extended with:
  - REQ-SPEC-0004 (req-id-format)
  - REQ-SPEC-0005 (spec-frontmatter-valid)
  - REQ-SPEC-0006 (compound-requirement-detector)
  - REQ-SPEC-0007 (prose-xref-banned)
  - REQ-SPEC-0008 (xref-resolves)
  - REQ-SPEC-0009 (anti-aliasing)
  - REQ-SPEC-0010 (req-id-immutable)
  - REQ-SPEC-0011 (req-append-only)
  - REQ-SPEC-0012 (spec_lint infrastructure)
  - REQ-SPEC-0013 (annotation discipline)
- [ ] 5 tests in `test_mutations.py` get new `@test-id` annotations
      following the existing `TEST-SPEC-MUT-<DOMAIN>-NNN` convention,
      plus `@covers REQ-SPEC-NNNN` matching the rule each tests.
- [ ] 79 tests across the suite get `@covers REQ-SPEC-NNNN` added per
      the file-to-REQ mapping in `CHG-0030/proposal.md`.
- [ ] 3 tests using legacy `@covers (rule: <name>)` form convert to
      `@covers REQ-SPEC-NNNN` form (in `test_req_id_format.py` and
      `test_spec_frontmatter_valid.py` per the audit data).
- [ ] Meta-test in `test_annotation_discipline.py` updated to remove
      its self-exemption — it now asserts on itself too.
- [ ] `pytest tools/` → all-green; the meta-test's RED from TASK-0027
      flips to green with zero violations remaining.
- [ ] `python -m tools.spec_lint validate openspec` → exit 0 (no new
      defects introduced by the spec extension).
- [ ] Commit trailer `Tests-Status: red→green`.

## Notes

- The 10 new REQs all declare `status: tests-green` because their
  covering tests EXIST in the prior commit (TASK-0027 ships the
  meta-test; the per-rule tests existed before). Per the P2 audit
  finding, this is the existing pattern; an ADR clarification is queued
  for CHG-0028.
- The backfill is mechanical: per-file, one REQ-SPEC ID per file
  (except `test_mutations.py` which has per-test mapping). Easier to
  review file-by-file than test-by-test.
- This task may produce a large GREEN diff (~92 docstring edits + 10
  REQ blocks). Acceptable scope because the alternative (8+ smaller
  CHGs) would each leave a discipline gap open between PRs.
