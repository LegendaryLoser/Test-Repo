---
id: TASK-0015
type: impl
status: pending
covers: []
dependencies:
  - TASK-0014
---

# TASK-0015 — Implement mutation runner; surface defects

## Intent

Replace the NIE stubs in `runner.py` with working implementations. Run the
driver tests. If any must-catch mutation survives, surface it as a test
failure and queue a fix task.

## Definition of done

- [ ] `runner.run_mutation(seed, mutation, rule)` returns
      `(mutated_text, findings)`.
- [ ] `runner.assert_caught(mutation, findings)` raises with a clear
      message if no finding from `mutation.expected_rule_id` is present.
- [ ] `pytest tools/` runs end-to-end. Either:
      (a) all must-catch tests pass → CHG-0008 ready to merge, OR
      (b) some must-catch tests fail → each failure is documented in a
      follow-up TASK-0016+ within this CHG, with the fix landing in its
      own red→green commit pair.
- [ ] Total runtime stays under 10 seconds.

## Notes

- Commit trailer for runner impl:
  `Task: TASK-0015`, `Requirements: -`, `Tests-Status: red→green`,
  `Phase: PHASE-1`.
- Push after the commit.
