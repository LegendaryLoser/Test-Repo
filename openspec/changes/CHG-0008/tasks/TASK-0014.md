---
id: TASK-0014
type: test-red
status: pending
covers: []
dependencies:
  - TASK-0013
---

# TASK-0014 — Scaffold mutation framework + corpora + driver (RED)

## Intent

Create the framework module, all per-rule mutation corpora, and the driver
test. Framework `run_mutation` raises `NotImplementedError`; driver tests
fail at the call.

## Definition of done

- [ ] `tools/spec_lint/tests/mutations/__init__.py` exists.
- [ ] `tools/spec_lint/tests/mutations/_models.py` defines `Mutation`
      dataclass with `id, description, expected_rule_id, apply, category`.
- [ ] `tools/spec_lint/tests/mutations/runner.py` exposes `run_mutation`
      (RED stub) and `assert_caught` (RED stub).
- [ ] Six mutation-corpus modules exist with full mutation lists per the
      proposal:
      `req_id_format_mutations.py`,
      `spec_frontmatter_valid_mutations.py`,
      `compound_mutations.py`,
      `anti_aliasing_mutations.py`,
      `xref_resolves_mutations.py`,
      `prose_xref_banned_mutations.py`.
- [ ] `tests/test_mutations.py` parametrizes over each corpus's must-catch
      mutations and asserts each is caught via `run_mutation` +
      `assert_caught`. Known-limitation mutations are iterated separately
      and only logged.
- [ ] `pytest tools/` shows failures only on the new mutation tests; all
      prior 85 tests still pass.

## Notes

- Commit trailer: `Task: TASK-0014`, `Requirements: -`, `Tests-Status: red`,
  `Phase: PHASE-1`.
- Push immediately after commit.
