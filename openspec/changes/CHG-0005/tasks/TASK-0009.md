---
id: TASK-0009
type: impl
status: pending
covers: []
dependencies:
  - TASK-0008
---

# TASK-0009 — Implement prose-xref-banned + xref-resolves

## Intent

Replace the NIE stubs from TASK-0008 with working implementations. All tests
turn green.

## Definition of done

- [ ] `ProseXrefBanned.check_files(files)`:
  - Walks each (path, text) line by line.
  - Tracks fenced code blocks (``` openings/closings) and skips lines inside.
  - Skips lines containing `spec-lint: allow prose-xref-banned`.
  - For each denylist pattern, if a match is found and the same line does
    NOT contain a stable ID after the match, yields a Finding.
- [ ] `XrefResolves.check_files(files)`:
  - For each markdown link `[text](https://example.com/target)`-style where
    target is relative (not http/https/mailto and not a pure `#fragment`):
    resolve relative to path.parent and yield Finding if it does not exist.
  - For each match of pattern `REQ-<DOMAIN>-<NNNN> @ <relative/path>`:
    resolve the path; yield Finding if path missing; yield Finding if path
    exists but does not contain `## REQ-<DOMAIN>-<NNNN>`.
- [ ] All findings carry rule_id, severity "error", file (str), line, and
      a descriptive message.
- [ ] `pytest tools/spec_lint/ tools/ci/` is all-green.

## Notes

- Commit trailer:
  - `Task: TASK-0009`
  - `Requirements: -`
  - `Tests-Status: red→green`
  - `Phase: PHASE-1`
- Push immediately after the commit.
