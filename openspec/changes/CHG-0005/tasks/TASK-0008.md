---
id: TASK-0008
type: test-red
status: pending
covers: []
dependencies:
  - TASK-0007
---

# TASK-0008 — Scaffold prose-xref-banned + xref-resolves stubs + RED tests

## Intent

Add stubs for both cross-file rules that raise `NotImplementedError`, plus
real test bodies and a tmp_path-based helper. P4-compliant red history before
TASK-0009 fills the implementations.

## Definition of done

- [ ] `tools/spec_lint/rules/prose_xref_banned.py` exists; `check_files`
      raises NIE.
- [ ] `tools/spec_lint/rules/xref_resolves.py` exists; `check_files` raises NIE.
- [ ] `tools/spec_lint/tests/xref_helpers.py` exposes `write_doc(root, rel,
      text)` to lay out files in a tmp_path tree.
- [ ] `tools/spec_lint/tests/test_prose_xref_banned.py` covers: inline-text
      clean cases (no prose patterns), inline-text dirty cases (`the X spec`, `the X requirement`, `the X ADR`), code-block exclusion, inline allow marker, pattern-followed-by-ID exemption. <!-- spec-lint: allow prose-xref-banned -->
- [ ] `tools/spec_lint/tests/test_xref_resolves.py` covers: valid markdown
      link, broken markdown link, http/mailto skip, REQ-ID@path resolves to
      a spec file containing the REQ, REQ-ID@path where path is missing,
      REQ-ID@path where path exists but does not contain the REQ.
- [ ] `pytest tools/spec_lint/` shows failures for every new behavior test;
      attribute-check tests pass.
- [ ] `pytest tools/ci/` still 8/8.

## Notes

- Commit trailer:
  - `Task: TASK-0008`
  - `Requirements: -`
  - `Tests-Status: red`
  - `Phase: PHASE-1`
- Push immediately after commit (ADR-0005 §6 amendment 0001).
