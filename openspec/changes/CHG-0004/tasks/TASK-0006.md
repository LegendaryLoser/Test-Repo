---
id: TASK-0006
type: test-red
status: pending
covers: []
dependencies:
  - TASK-0005
---

# TASK-0006 — Scaffold git_history adapter + 2 rule stubs + RED tests

## Intent

Establish the substrate for history-aware rules: the `git_history` adapter,
the `HistoricalRule` protocol, and real test bodies whose only failure mode
is the stubs raising `NotImplementedError`. P4-compliant red history before
TASK-0007 fills in implementations.

## Definition of done

- [ ] `tools/spec_lint/git_history.py` exists; `historical_versions()` raises
      `NotImplementedError("...RED stub...")`.
- [ ] `tools/spec_lint/rules/base.py` extended with `HistoricalRule`
      Protocol.
- [ ] `tools/spec_lint/rules/req_id_immutable.py` exists; `check_history()`
      raises NIE.
- [ ] `tools/spec_lint/rules/req_append_only.py` exists; `check_history()`
      raises NIE.
- [ ] `tools/spec_lint/tests/history_helpers.py` exposes
      `make_repo_with_history(tmp_path, file_rel, versions)`. Uses real
      subprocess against real git. No mocks.
- [ ] `tools/spec_lint/tests/test_git_history.py` has real test bodies
      covering: empty history, single commit, multiple commits, oldest→newest
      ordering, parsed `SpecFile` matches `parse_spec_file` on the same
      content.
- [ ] `tools/spec_lint/tests/test_req_id_immutable.py` has scenarios:
      unchanged history, deprecation-in-place, removal, rename.
- [ ] `tools/spec_lint/tests/test_req_append_only.py` has scenarios:
      never-green, frozen-after-green, revision-bump-with-body-change,
      silent-body-edit-after-green.
- [ ] `pytest tools/spec_lint/` reports failures for every new test except
      attribute-check tests.
- [ ] `pytest tools/ci/` still 8/8.

## Notes

- Commit trailer:
  - `Task: TASK-0006`
  - `Requirements: -`
  - `Tests-Status: red`
  - `Phase: PHASE-1`
- Push immediately after the commit (ADR-0005 §6 amendment 0001).
