---
id: TASK-0003
type: docs
status: pending
covers:
  - REQ-ARCH-0008
dependencies: []
---

# TASK-0003 — Amend ADR-0005 (push invariant) and update PHASE-2 scope

## Intent

Author the textual amendment to
[`ADR-0005`](../../../architecture/decisions/ADR-0005-traceability-and-journaling.md)
that introduces the per-commit and per-merge push invariant. Implementation
of the invariant remains PHASE-2 work; this task is the **paper change** that
locks the intent before any further PHASE-1 work proceeds.

## Definition of done

- [ ] `ADR-0005` §6 hook table updated:
  - `PostToolUse(Bash:git commit)` row amended to include push of `HEAD`
    (FF-only; non-FF aborts with diagnostic).
  - New row `PostToolUse(mcp__github__merge_pull_request)` →
    `tools/trace/post_merge_sync.py`.
  - `Stop` / `SessionEnd` row amended to include "push committed work first".
- [ ] `ADR-0005` §6 gains a paragraph naming the push invariant explicitly.
- [ ] `ADR-0005` §7 gains a paragraph extending the checkpoint hook to push
      committed-but-unpushed work when the tree is clean.
- [ ] `ADR-0005` §8 failure-mode table gains a row for
      "container reclaim, committed but unpushed".
- [ ] `ADR-0005` §10 amendment log created with row 0001 (this CHG).
- [ ] `PHASE-2` scope (item 1) lists `post_merge_sync.py` and notes the
      extended responsibilities of `journal_commit.py` and `checkpoint.py`.
- [ ] `PHASE-2` scope (item 2) lists push-invariant tests.
- [ ] `.claude/settings.json` declares the
      `PostToolUse:mcp__github__merge_pull_request` matcher and updates
      TODO comments on the existing matchers to reference the new behaviors.
- [ ] PHASE-0 verification tests still pass:
      `pytest tools/ci/tests/test_phase0.py` → 8 passed.

## Notes

- Commit trailer:
  - `Task: TASK-0003`
  - `Requirements: REQ-ARCH-0008`
  - `Tests-Status: none`
  - `Phase: PHASE-0`
- `REQ-ARCH-0008` is the natural reference target: it constrains
  `.claude/settings.json` to declare every hook from
  `ADR-0005` §6. Amending §6 implicitly extends what `REQ-ARCH-0008`
  demands, and this PR updates `settings.json` to keep the declaration
  consistent with ADR-0005 post-amendment.
- No new REQ is introduced. The behavioral invariant is documented inside
  `ADR-0005` itself; PHASE-2 will create a CI gate that verifies the
  hooks implement what the ADR specifies (separate task in PHASE-2 CHG).
