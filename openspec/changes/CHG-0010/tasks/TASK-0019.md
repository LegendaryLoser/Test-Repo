---
id: TASK-0019
type: impl
status: pending
covers: []
dependencies:
  - TASK-0018
---

# TASK-0019 — Install BMAD v6.6.0 (vendored); will leave TEST-ARCH-0001 RED

## Intent

Run the pinned BMAD install in this repo; vendor the resulting `_bmad/` +
`.claude/skills/` to git; clean up empty side-effect directories; commit
the install script for reproducibility.

This commit knowingly puts TEST-ARCH-0001 into RED state because the
allowlist still says `bmad` not `_bmad`. TASK-0020 amends the allowlist
(and the ADRs that reference it) and returns the suite to GREEN.

## Definition of done

- [ ] `scripts/install_bmad.sh` exists with the pinned install command.
- [ ] `_bmad/` directory present (~172 KB, version 6.6.0 in manifest).
- [ ] `.claude/skills/` populated with 42 skills (~2.4 MB).
- [ ] `openspec/_bmad-output/.gitkeep` present.
- [ ] Empty `docs/` removed.
- [ ] Empty `openspec/_bmad-output/{planning-artifacts, implementation-artifacts}/`
      removed.
- [ ] `pytest tools/` runs; expected state: 156 passed, 1 failed
      (TEST-ARCH-0001). All other tests still green.

## Notes

- Commit trailer: `Task: TASK-0019`, `Requirements: REQ-ARCH-0001`,
  `Tests-Status: red`, `Phase: PHASE-1`.
- Push immediately after commit.
- The push invariant means the RED state briefly lives on origin until
  TASK-0020 lands — acceptable per the red-first protocol.
