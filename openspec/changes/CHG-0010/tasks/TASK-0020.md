---
id: TASK-0020
type: docs
status: pending
covers:
  - REQ-ARCH-0001
  - REQ-ARCH-0002
dependencies:
  - TASK-0019
---

# TASK-0020 — Amend ADR-0002, ARCHITECTURE.md, TEST-ARCH-0001 allowlist

## Intent

Reconcile the architecture with the BMAD install reality. Three
substrate amendments. After this commit, the entire test suite is GREEN.

## Definition of done

- [ ] [`ADR-0002`](../../../architecture/decisions/ADR-0002-bmad-integration.md)
      amended (amendment 0001 in §6 amendment log):
  - Install path: `_bmad/` not `bmad/`.
  - Integration: native Claude Code Skills in `.claude/skills/` (drop
    wrapper isolation; describe what BMAD actually does).
  - Routed output configuration captured.
  - `bmad-direct-reference` lint rule must check for `_bmad/` paths;
    wrappers under `.claude/agents/` and `.claude/commands/` no longer
    apply.
- [ ] [`ARCHITECTURE.md` §3](../../../architecture/ARCHITECTURE.md):
  - Replace `bmad/` with `_bmad/` in the layout tree.
  - Add `.claude/skills/` to the `.claude/` listing.
  - Add `openspec/_bmad-output/` to the `openspec/` listing.
  - Add `scripts/` as a top-level entry.
- [ ] `tools/ci/tests/test_phase0.py` `ALLOWED_TOP_LEVEL_ENTRIES`:
  - Remove `bmad`.
  - Add `_bmad`.
  - Add `scripts`.
- [ ] `pytest tools/` → 157+ passed (all green).
- [ ] All cross-references inside `openspec/architecture/` still resolve.

## Notes

- Commit trailer: `Task: TASK-0020`, `Requirements: REQ-ARCH-0001, REQ-ARCH-0002`,
  `Tests-Status: red→green`, `Phase: PHASE-1`.
- Push after the commit.
