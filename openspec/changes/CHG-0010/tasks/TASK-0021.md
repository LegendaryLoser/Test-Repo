---
id: TASK-0021
type: docs
status: pending
covers:
  - REQ-ARCH-0001
dependencies:
  - TASK-0020
---

# TASK-0021 — ADR-0002 amendment 0002: staging discipline + upgrade safeguards + per-project isolation

## Intent

Address the SoT defect surfaced in chat: `openspec/_bmad-output/` as
currently configured would create a second home for artifact types whose
SoT is `openspec/{briefs,prd,epics,stories,architecture}/`. The
amendment establishes `_bmad-output/` as **STAGING** (non-authoritative)
with hard invariants enforced by lint/matrix machinery, formalizes
**upgrade safeguards**, and documents **per-project output isolation**
which was empirically verified reliable against the vendored BMAD
resolver.

## Definition of done

- [ ] `openspec/_bmad-output/STAGING.md` exists, documenting the
      promotion protocol and what lint rules see (and don't see).
- [ ] [`ADR-0002`](../../../architecture/decisions/ADR-0002-bmad-integration.md)
      gains §7 (staging discipline with hard invariants), §8 (upgrade
      safeguards: smoke test, skill-removal acknowledgment, rollback),
      §9 (per-project isolation via BMAD's 4-layer config merge).
      Amendment 0002 row in the amendment log.
- [ ] [`ARCHITECTURE.md` §3](../../../architecture/ARCHITECTURE.md)
      labels `openspec/_bmad-output/` as STAGING with reference to
      ADR-0002 §7.
- [ ] [`PHASE-2`](../../../architecture/phases/PHASE-2-traceability-runtime.md)
      scope binds: `matrix-drift` MUST exclude `_bmad-output/`;
      `skill-removal-acknowledged`, `bmad-version-pin`, `bmad-smoke-test`
      gates added.
- [ ] [`PHASE-5`](../../../architecture/phases/PHASE-5-first-appscript-project.md)
      scope binds: per-project `_bmad/custom/config.toml` setup is an
      explicit deliverable; first project verifies BMAD picks up the
      override empirically.
- [ ] All architecture cross-references still resolve.
- [ ] `pytest tools/` still 157/157.

## Notes

- Commit trailer: `Task: TASK-0021`, `Requirements: REQ-ARCH-0001`,
  `Tests-Status: red→green` (no new tests; "green" = no regression),
  `Phase: PHASE-1`.
- Push after the commit.
- Per-project isolation is **reliable today** (verified against
  `_bmad/scripts/resolve_config.py`'s 4-layer merge), not deferred. The
  *first exercise* lands in PHASE-5 with the first concrete project,
  which is the natural place to verify end-to-end.
