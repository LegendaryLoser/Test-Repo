---
id: TASK-0029
type: test
status: pending
covers:
  - REQ-SPEC-0014
dependencies: []
---

# TASK-0029 — RED meta-test for CI workflow wiring

## Intent

Ship the test that asserts `.github/workflows/ci.yml`'s `spec-lint` job
actually invokes the gates. RED first per P4: the test exists in a
failing state in this commit; TASK-0030 GREEN rewrites the workflow to
satisfy the assertions.

## Definition of done

- [ ] `tools/ci/tests/test_workflow_wiring.py` exists with a single
      test `test_spec_lint_job_invokes_real_gates`.
- [ ] Test parses `.github/workflows/ci.yml` as YAML and asserts:
  - The workflow triggers on `pull_request` and `push` to
    master/main.
  - The `spec-lint` job exists.
  - The job's steps include (in order or anywhere): checkout,
    `actions/setup-python` with python-version `3.11`+, a dependency
    install step (`pip install -e ".[dev]"`-shaped), and runs of
    `python -m tools.spec_lint validate`, `python -m tools.spec_lint
    check-layout`, and `pytest tools/`.
  - No step's `run:` field contains `TODO PHASE-1` (the placeholder
    marker).
- [ ] Annotations: `@test-id TEST-SPEC-0100`, `@covers REQ-SPEC-0014`
      (forward-referenced; REQ-SPEC-0014 lands in TASK-0030).
- [ ] `pytest tools/ci/tests/test_workflow_wiring.py` shows
      assertion-red (specific violations enumerated), NOT collection-red.
- [ ] Full suite shows the new test failing + all 179 others passing.
- [ ] Commit trailer `Tests-Status: red`.

## Notes

- pyyaml is already a dev dep (used by CHG-0001 onward).
- The test does not run the workflow — it asserts the workflow's
  structural contract. Live CI runs are observed via PR results, not
  unit tests.
