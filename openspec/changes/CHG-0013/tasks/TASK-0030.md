---
id: TASK-0030
type: impl
status: pending
covers:
  - REQ-SPEC-0014
dependencies:
  - TASK-0029
---

# TASK-0030 — GREEN: rewrite `spec-lint` job + add REQ-SPEC-0014

## Intent

Replace TASK-0029's RED state by:
1. Rewriting the `spec-lint` job in `.github/workflows/ci.yml` with
   real steps invoking the spec_lint CLI + pytest.
2. Adding REQ-SPEC-0014 to `openspec/specs/_meta/spec-storage.spec.md`.

## Definition of done

- [ ] `.github/workflows/ci.yml`'s `spec-lint` job has steps:
  1. Checkout (with `fetch-depth: 0` for history-based rules).
  2. Setup Python 3.11.
  3. Install dev dependencies.
  4. `python -m tools.spec_lint validate openspec`.
  5. `python -m tools.spec_lint check-layout`.
  6. `pytest tools/ -v`.
  Each step has a `name:` for log readability.
- [ ] `trace-gates`, `unit-integration`, `phase-exit` jobs are
      UNCHANGED — they remain placeholders pending PHASE-2 / PHASE-3+.
- [ ] `openspec/specs/_meta/spec-storage.spec.md` extended with
      REQ-SPEC-0014.
- [ ] Workflow file is valid YAML (PyYAML parse succeeds).
- [ ] `pytest tools/` → all-green; TASK-0029's RED test now passes.
- [ ] `python -m tools.spec_lint validate openspec` → exit 0 (no
      defects introduced by the spec extension).
- [ ] Commit trailer `Tests-Status: red→green`.

## Notes

- The workflow can only be empirically verified by an actual CI run
  against this PR. The meta-test verifies the CONTRACT (correct
  invocations declared); the CI run verifies the EXECUTION.
- If the first CI run surfaces unexpected defects (e.g., environment
  drift between local and CI runners), fix in a follow-up commit
  on this branch — do not weaken the meta-test to accommodate.
