---
id: TASK-0032
type: impl
status: pending
covers:
  - REQ-SPEC-0015
dependencies:
  - TASK-0031
---

# TASK-0032 — GREEN: implement `index` + populate INDEX.yaml

## Intent

Replace TASK-0031's `NotImplementedError` skeletons with real
implementations. Regenerate `openspec/specs/INDEX.yaml` to contain
all 14 REQ-SPEC entries. Land REQ-SPEC-0015.

## Definition of done

- [ ] `tools/spec_lint/index.py`:
  - `build_index_data(specs_root, repo_root)` walks `specs_root` for
    `*.spec.md`, parses each, extracts every REQ block with valid
    frontmatter, returns a dict with `version: 1`, `generated_at:
    <ISO8601>`, `requirements: [...]` (sorted by ID).
  - `write_index(specs_root, repo_root, target)` serializes via
    `yaml.safe_dump(..., sort_keys=False, default_flow_style=False)`
    and writes to `target`.
  - `check_index(specs_root, repo_root, target)` reads `target`,
    compares against `build_index_data()` ignoring `generated_at`,
    returns 0 / 1 / 2 per the proposal.
- [ ] `tools/spec_lint/__main__.py` `index` subcommand fully wired.
- [ ] `openspec/specs/INDEX.yaml` regenerated, contains 14 entries
      (REQ-SPEC-0001..0014). File committed to git.
- [ ] `openspec/specs/_meta/spec-storage.spec.md` extended with
      `REQ-SPEC-0015`. Atomic acceptance (one Given/When/Then) to avoid
      the same `compound-requirement-detector` trap from CHG-0013.
- [ ] `python -m tools.spec_lint index --check` → exit 0.
- [ ] `python -m tools.spec_lint validate openspec` → exit 0.
- [ ] `python -m tools.spec_lint check-layout` → exit 0.
- [ ] `pytest tools/` → all-green; TASK-0031's 4 RED tests now pass.
- [ ] Commit trailer `Tests-Status: red→green`.

## Notes

- Index entries land in deterministic order — sorted by REQ ID.
- The `generated_at` timestamp is a real ISO8601 (UTC, second
  precision). It will differ on every regeneration, but `--check`
  ignores it so this does not cause false failures.
- REQ-SPEC-0015 declares `status: tests-green` because its covering
  tests existed (in `tests-red` state) in TASK-0031. Same pattern as
  every previous REQ-SPEC.
