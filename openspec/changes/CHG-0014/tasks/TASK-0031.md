---
id: TASK-0031
type: test
status: pending
covers:
  - REQ-SPEC-0015
dependencies: []
---

# TASK-0031 — RED tests for `index` subcommand

## Intent

Ship skeleton `tools/spec_lint/index.py` + failing tests per P4.
The skeleton's functions raise `NotImplementedError` so every test
exercising them fails-by-error in this commit; TASK-0032 GREEN
replaces with real implementations.

## Definition of done

- [ ] `tools/spec_lint/index.py` exists with three function stubs
      (`build_index_data`, `write_index`, `check_index`) all raising
      `NotImplementedError`.
- [ ] `tools/spec_lint/__main__.py` adds an `index` subcommand with
      `--check` flag that invokes the stub.
- [ ] `tools/spec_lint/tests/test_index.py` exists with at least four
      tests covering: build over a tmp_path spec file, build over the
      real repo (expects 14 REQ-SPEC entries), check detects a planted
      stale INDEX, real-repo `check_index` exits 0 (this last is the
      ultimate green target).
- [ ] All four new tests fail in this commit (any combination of
      assertion-red and error-red is acceptable provided the test is
      failing).
- [ ] Full suite: only the four new tests fail; no regression.
- [ ] Commit trailer `Tests-Status: red`.

## Notes

- Annotations: `@test-id TEST-SPEC-0101..0104`, `@covers REQ-SPEC-0015`
  (forward-referenced; spec lands in TASK-0032).
- The CLI subcommand stub is necessary so that future subprocess-based
  tests (if any) can invoke `python -m tools.spec_lint index` without
  argparse errors. The stub raises NIE which surfaces as a non-zero
  exit code.
