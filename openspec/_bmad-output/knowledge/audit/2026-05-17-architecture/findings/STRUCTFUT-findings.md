# STRUCTFUT — editorial-review-structure-forward-transformations (opus) — Wave 7

**Source transcript:** [`raw-transcripts/STRUCTFUT-agent-a9bbc43cc082fa7eb.jsonl`](../raw-transcripts/STRUCTFUT-agent-a9bbc43cc082fa7eb.jsonl)
**Sub-agent description:** `Structural forward transformations`
**Session ID:** `2fa6b8f3-f53b-44bf-873e-44bdb568369b`
**Agent ID:** `a9bbc43cc082fa7eb`

## Initial prompt to sub-agent

```
You are an isolated reviewer with no prior context. Your method is **forward-looking structural review** — you assess the architecture's STRUCTURE under the pressures it will face as it accumulates ADRs, amendments, REQs, gates, hooks, and CHGs over the next 2-3 years. NOT a content review (other streams handle that); NOT a current-state structural review (Wave 1's STRUCT and Wave 6's STRUCTGOV handled that); your focus is what structural transformations the artifact set needs to absorb predictable growth.

Documents to review (under /home/user/Test-Repo/):
- openspec/architecture/ARCHITECTURE.md
- All 8 ADRs in openspec/architecture/decisions/
- All 6 PHASE files in openspec/architecture/phases/

Focus axes (forward structural pressure):

1. **ADR amendment accumulation.** Two ADRs (ADR-0002, ADR-0005) already have amendment logs. As more ADRs accumulate amendments, what structural transformations does the directory layout need? Filename convention? Section structure? Reference resolution? Will the amendment-log-as-table pattern scale?
2. **REQ-ID space.** Multiple domains exist (REQ-ARCH, REQ-SPEC, REQ-CI, REQ-LLM, REQ-LIBTS, REQ-LIBPY, REQ-AUDIT). At 100, 500, 1000 REQs how does the storage discipline (ADR-0004) hold up? At what scale does the per-domain numbering collide / drift / get re-organised?
3. **Gate inventory growth.** ADR-0008 §1 currently lists ~30 gates. At 100 gates how does the table scale? At what point does the inventory need a sub-inventory? Does `gate-coverage`'s walking-cost grow problematically?
4. **CHG accumulation.** Existing CHG numbers span 0001-0042 across the corpus. At 200, 1000 CHGs how does the directory structure (`openspec/changes/<CHG>/`) scale? Discoverability? Search? Is alphanumeric flat-listing tractable?
5. **Amendment-vs-supersession.** As ADRs accumulate amendments, when does the architecture need to force supersession over continued amendment? Is there a structural threshold (e.g., "after N amendments, supersede")? PERSRETRO-PROC-004 (Wave 6) flagged that 8-amendment ADRs accreted into unreadable diffs in a future scenario.
6. **Cross-reference graph density.** As more cross-references accumulate (`ADR-X §Y`, `REQ-Z`, `THEME-Q`, etc.), at what density does the graph become navigation-impractical? Does the architecture have an `xref-graph` artifact? Should it?
7. **Vendored substrate evolution.** BMAD is vendored at v6.6.0. The architecture has a substrate-PR pattern but no rule for parallel-version vendoring (e.g., testing v7 alongside v6.6.0). What structure is needed?
8. **Methodology codification's structural fit.** The methodology codification ADR is queued. What is its likely structural shape (single ADR? ADR cluster? sub-directory)? Where in the directory hierarchy does it land? What companion artifacts (test suites, calibration data) need structural homes?

Out of scope:
- Content defects (other streams).
- Current-state structural defects (STRUCT, STRUCTGOV cover those).
- Forward-looking failure narratives (PREM, RED handle).
- Forward-looking scenario unfolds (Wave 7 SCENFUT companion stream handles).

REQUIRED OUTPUT:
- Finding IDs prefixed `STRUCTFUT-` with severity inline (e.g., `STRUCTFUT-CRIT-001`).
- Severity guide: CRIT = the structure will break under predictable growth without ADR amendment now; SER = the structure will degrade meaningfully without ADR amendment within the foreseeable horizon; PROC = forward structural documentation gap.
- Minimum 12 findings, target 14-18.
- Each finding cites a specific artifact (file + section), describes the predictable growth pressure, and identifies the structural transformation needed.
- 1200-1800 words.
- NO preamble. Start directly with finding STRUCTFUT-CRIT-001 (or SER-001 if no CRIT).
- End with a one-paragraph summary that totals by severity and names the structural dimension under most predictable pressure.

Do not read files outside the architecture documents listed.
```

## Findings deliverable (final assistant message)

Now I have all the architectural artifacts loaded. Let me produce the forward-looking structural review.

STRUCTFUT-CRIT-001 — Flat `decisions/` directory cannot absorb ADR growth past ~50 entries with current naming. *Artifact:* `openspec/architecture/decisions/` per `ARCHITECTURE.md` §3 and ADR-0001 §"Decision". *Pressure:* The directory currently holds 8 ADRs as `ADR-NNNN-<slug>.md`. At the documented amendment cadence (already ADR-0002 carries 2 amendments and ADR-0005 carries 1 after only weeks of life), and with PHASE-5's commitment that "any pain point... results in an ADR amendment, not a workaround," ADR count plus per-ADR amendment count will balloon. At 50+ ADRs, file listings collapse alphabetically with no domain partition (CI gates, storage, runtime, testing, vendoring all interleave). *Transformation:* The directory needs a thematic sub-partition (`decisions/storage/`, `decisions/ci/`, `decisions/runtime/`, `decisions/vendoring/`, `decisions/methodology/`) introduced *before* the 20th ADR, plus an ADR index file (`decisions/INDEX.yaml`) governed analogously to `openspec/specs/INDEX.yaml`. The architecture document must be amended to make domain-partitioning a structural rule with its own ADR.

STRUCTFUT-CRIT-002 — Amendment-log-as-markdown-table breaks readability past ~6 amendments. *Artifact:* ADR-0002 §"Amendment log" and ADR-0005 §10. *Pressure:* Both amendment logs already use a four-column markdown table whose rightmost "Summary" column wraps to multi-paragraph cells. ADR-0002 amendment 0002 already spans 5 logical changes packed into one table row. PERSRETRO-PROC-004's scenario (8-amendment ADRs becoming unreadable) is on-trajectory; the table form will not even render correctly in standard markdown viewers past ~6 long-summary rows. *Transformation:* Convert amendment logs from a table into a structured per-amendment sub-document — either an `amendments/` sibling directory (`decisions/ADR-0005/amendments/AMD-0001.md`) or a YAML frontmatter array referenced from a rendered HTML log. The ADR itself becomes a directory, not a single file. This is a load-bearing structural change and warrants its own ADR.

STRUCTFUT-CRIT-003 — Amendment-vs-supersession threshold is undefined; the architecture lacks a forcing function. *Artifact:* ADR-0002 §"Amendment log" closing paragraph ("Amendments are append-only. A subsequent material change... requires a new amendment row, never an edit") and the parallel paragraph in ADR-0005 §10. *Pressure:* Both ADRs declare amendments append-only but never specify when an ADR must be superseded rather than amended. The frontmatter has `supersedes` / `superseded_by` slots, but no structural rule converts an N-amendment ADR into a supersession candidate. Without a threshold, an ADR will accumulate amendments indefinitely until its original text disagrees with its present behavior — exactly what PERSRETRO-PROC-004 flagged. *Transformation:* Add an ADR (e.g. ADR-0009) codifying a hard structural threshold — for example, "≥3 amendments OR amendment-byte-count > original-byte-count forces supersession" — plus a CI gate `amendment-saturation` whose ownership row joins ADR-0008 §1. Without this gate, the existing "append-only" rule actively *prevents* the cleanup mechanism that supersession would supply.

STRUCTFUT-SER-004 — REQ-DOMAIN namespace has no allocation registry; collision and drift are inevitable at scale. *Artifact:* ADR-0004 §1 ("DOMAIN is uppercase alphanumeric ≤ 12 chars") and ARCHITECTURE.md §4 identifier table. *Pressure:* Domains observed in the corpus already include ARCH, SPEC, CI, LLM, LIBTS, LIBPY, AUDIT, plus per-project domains implied by PHASE-5. At 500-1000 REQs across 20+ projects, two teams will independently pick `REQ-AUTH-*`, `REQ-USR-*`, etc. for different features. The ≤12-char namespace alone cannot prevent collision; INDEX.yaml is per-domain monotonic only *if* domains are coordinated. *Transformation:* Add a `openspec/specs/DOMAINS.yaml` registry, hand-authored, governed by an ADR with its own `domain-registry-valid` lint gate. Domain allocation becomes a coordination step before any REQ can be allocated to a new domain. Without this, the per-domain monotonic counter rule degrades into per-team monotonic-counters that overlap on merge.

STRUCTFUT-SER-005 — ADR-0008 §1 gate inventory is a flat markdown table; it cannot scale past ~60 entries. *Artifact:* ADR-0008 §1 "Gate inventory" table. *Pressure:* The table currently lists 26 gates. PHASE-2 already adds at least 3 more (`skill-removal-acknowledged`, `bmad-version-pin`, `bmad-smoke-test`). Each subsequent ADR amendment tends to add at least one gate (the architecture promises `stale-staging` is "deferred"). At 100 gates, a single markdown table is unreadable, ungroupable, and ungovernable; `gate-coverage`'s walking cost (parse markdown table → cross-walk ADRs/principles) grows linearly with both ADR count and gate count, becoming O(N×M). *Transformation:* Convert §1 into a structured `openspec/architecture/gates/INDEX.yaml` plus per-gate stub files (`gates/<gate-id>.gate.md`) with frontmatter, similar to spec storage. The markdown table in §1 becomes a *rendered* artifact, not the source. Gate-coverage walks YAML, not markdown.

STRUCTFUT-SER-006 — CHG numbering is flat alphanumeric with no sharding; `openspec/changes/` becomes a swamp past ~500 entries. *Artifact:* ARCHITECTURE.md §4 (`CHG-<NNNN>` → `openspec/changes/<CHG-NNNN>/`) and ADR-0005 §"Status transition rules". *Pressure:* CHG numbers in the corpus span 0001-0042 already after roughly weeks of activity. At a sustained cadence of ~5 CHGs/week, 1000 CHGs land within ~4 years. Flat `ls openspec/changes/` of 1000+ directories becomes unusable for discovery; git status and CI walkers degrade; the directory becomes write-contention prone. *Transformation:* Introduce structural sharding — e.g., `openspec/changes/0000-0499/CHG-0042/`, or year-prefixed (`openspec/changes/2026/CHG-0042/`). Adopt this *before* 200 CHGs so the migration is small. Update ADR-0001 and §4 of ARCHITECTURE.md to bind the sharding rule, plus a `chg-sharding` lint gate.

STRUCTFUT-SER-007 — No cross-reference graph artifact exists; navigation cost is O(grep) over the whole corpus. *Artifact:* ADR-0004 §2 ("xref-resolves" gate) and ADR-0005 §9 (audit interface). *Pressure:* The xref-resolves gate verifies cross-references *resolve* but does not build a navigable graph. As the corpus grows to ~1000 REQs, ~50 ADRs, ~20 phases, ~thousands of CHGs, an agent wanting to answer "what depends on ADR-0005 §6 amendment 0001?" must grep the entire repo. The audit interface in ADR-0005 §9 handles ID-keyed audit but not cross-reference fan-in/fan-out. *Transformation:* Add an `openspec/traceability/xref-graph.yaml` as a derived artifact (analogous to matrix.yaml) built by `tools/trace/build_xref.py`. Add a `xref-graph-drift` gate. Define forward/backward navigation primitives in the audit CLI. Without this artifact, the architecture's "every reference is a stable ID + path" rule yields a giant unindexed adjacency list.

STRUCTFUT-SER-008 — Vendored substrate has no parallel-version structure; v6/v7 testing is structurally impossible. *Artifact:* ADR-0002 §1, §6 (single `_bmad/` install path, single pin in `manifest.yaml`). *Pressure:* The architecture commits to a single `_bmad/` directory with a single pinned version. When BMAD v7 becomes available, the team has no structural way to run the v7 smoke test in parallel with v6.6.0 production, no way to dual-vendored compare outputs, and no migration phase where both coexist. The "substrate PR" pattern assumes atomic version swap, which is acceptable for minor versions but catastrophic for major upgrades. *Transformation:* Amend ADR-0002 to introduce a `_bmad-staging/` parallel slot (or `_bmad/v6/`, `_bmad/v7/`) plus a `BMAD_ACTIVE_VERSION` config switch, governed by a phase-aligned ADR section. Add a `parallel-vendor-allowed` ADR to generalize the pattern to other vendored substrates (Claude Code Skills, etc.).

STRUCTFUT-SER-009 — Methodology codification ADR has no structural slot reserved. *Artifact:* ARCHITECTURE.md §1, §12 ("if it isn't an ADR, it isn't decided"); ADR-0004 §"Storage format" (REQ block structure used implicitly to encode method). *Pressure:* CLAUDE.md's session-guidance references a methodology codification ADR queued for landing. Methodology codification is intrinsically large and multi-faceted (rules + test suites + calibration data + decision tables). A single ADR will exceed the size of every existing ADR by an order of magnitude. The current decisions/ directory has no slot for companion artifacts (calibration data, test suites). *Transformation:* Reserve a structural slot now — `openspec/architecture/methodology/` with `INDEX.md`, `rules/`, `calibration/`, `tests/`. The methodology ADR becomes the *entry point* into a directory, not a single file. Decide before the methodology ADR lands whether it lives in `decisions/` (single ADR) or `methodology/` (ADR cluster). Document in ARCHITECTURE.md §3 so the slot exists ahead of need.

STRUCTFUT-SER-010 — Phase files are sequence-numbered with no slot for parallel or branching phases. *Artifact:* `openspec/architecture/phases/PHASE-N-*.md`; ARCHITECTURE.md §11. *Pressure:* The architecture commits to strict phase ordering and `phase-exit` enforces "no REQ from a later phase is tests-green." But PHASE-5's design ("any pain point results in an ADR amendment") implicitly admits feedback loops; a real PHASE-5 finding could require partial re-work of PHASE-2's gate set. The flat numbering also pre-commits to linear delivery; if PHASE-6+ introduces parallel workstreams (e.g., Python ML stream separate from the Apps Script stream), the structure has no room. *Transformation:* Generalize phase IDs to `PHASE-<MAJOR>.<MINOR>` (or `PHASE-<N>-<STREAM>`) and amend ADR-0008 §3 to allow stream-scoped exit criteria. Reserve the structural change now; the migration cost grows with each shipped phase.

STRUCTFUT-PROC-011 — `Amendments are append-only` paragraph is duplicated in ADR-0002 and ADR-0005 — a P1 (SoT) violation in the very documents that define P1. *Artifact:* ADR-0002 closing paragraph after amendment log; ADR-0005 §10 closing paragraph. *Pressure:* As more ADRs accumulate amendment logs, this paragraph gets copy-pasted into each one, drifting silently. The architecture's own SoT principle is currently violated by its governance prose. *Transformation:* Promote the "amendment discipline" prose into a single short ADR (or §11 of ADR-0005) and reference it from each amended ADR by `ID @ path`. Add a `prose-duplicate-banned` lint targeting paragraph-level n-gram similarity inside `openspec/architecture/decisions/`.

STRUCTFUT-PROC-012 — No structural home for ADR clusters that depend on each other. *Artifact:* ADR-0002 (which references ADR-0001, ADR-0005), ADR-0006 (references ADR-0007), ADR-0008 (references all others). *Pressure:* ADR-0008's `gate-coverage` walks every ADR; as ADR clusters form (e.g., a cluster on stochastic testing: ADR-0007 + future ADRs on rate-limit, on red-team tier, on cost-tier-budgeting), the dependency direction becomes a graph not a list. No structural artifact captures this graph. *Transformation:* Add `openspec/architecture/decisions/CLUSTERS.yaml` declaring named ADR clusters, or use frontmatter `cluster:` keys. `gate-coverage` walks clusters; a cluster missing a gate fails. Add this structural slot before the second cluster forms (likely PHASE-3 with stochastic-tier additions).

STRUCTFUT-PROC-013 — Test-ID and CHG-ID share the same flat `NNNN` space without scoping rule for ≥10K. *Artifact:* ARCHITECTURE.md §4 identifier table (`CHG-<NNNN>`, `TASK-<NNNN>`, `TEST-<DOMAIN>-<NNNN>`, `EPIC-<NNNN>`, `STORY-<NNNN>`). *Pressure:* `CHG-NNNN`, `TASK-NNNN`, `EPIC-NNNN`, `STORY-NNNN` all use four-digit padding. At sustained pace, TASK and TEST counts exceed 9999 first (each CHG carries multiple tasks; each REQ has ≥1 test). The padding format silently breaks at 10000. *Transformation:* Adopt variable-width padding (`NNNN+` with a minimum) or pre-commit to 6-digit padding now. Update ADR-0004 §1 to specify minimum padding and re-pin INDEX format. The migration is cheap today and expensive at 9000+.

STRUCTFUT-PROC-014 — `openspec/_bmad-output/` staging has no quota or sharding; staging accumulates indefinitely. *Artifact:* ADR-0002 §7 (staging discipline). *Pressure:* The `stale-staging` gate is "deferred CHG" — meanwhile drafts can accumulate without bound. At the BMAD invocation rate implied by PHASE-5+ multi-project work, the staging directory becomes thousands of orphan drafts. No structural sharding (no per-CHG sub-directory, no time-bucketing) exists. *Transformation:* Amend ADR-0002 §7 to require per-CHG sub-directories in staging (`_bmad-output/planning/CHG-<NNNN>/...`), enabling both `stale-staging` enforcement *and* easier promotion (one directory move = one CHG). Add `staging-sharded` lint.

STRUCTFUT-PROC-015 — ADR frontmatter has no `last-amended` or `amendment-count` field; tooling cannot detect saturation. *Artifact:* ADR-0001 through ADR-0008 frontmatter blocks (`id`, `title`, `status`, `date`, `supersedes`, `superseded_by`). *Pressure:* The frontmatter schema is fixed at PHASE-0; adding fields later requires re-validating every existing ADR. Without `amendments: N` or `last-amended: <date>` machine-readable, the supersession-threshold gate proposed in STRUCTFUT-CRIT-003 has no input. *Transformation:* Extend the frontmatter schema now to include `amendment_count: 0` and `last_amended: null` slots; back-fill the existing 8 ADRs in a single PHASE-0/1 commit. Adding fields after PHASE-1 lint hardens is much more expensive.

STRUCTFUT-PROC-016 — No structural artifact captures "deferred CHG" inventory; deferred work disappears. *Artifact:* ADR-0002 §7 references a "deferred CHG" for `stale-staging`; ADR-0002 §"Compliance" repeats. Multiple "(PHASE-N work)" forward references appear throughout. *Pressure:* These deferred items are tracked only by prose mention inside ADRs. As ADRs amend, deferral references drift; nothing fails CI when a deferred item is forgotten. *Transformation:* Add `openspec/architecture/deferred.yaml` listing every deferred item with its source ADR §, owner phase, and tracking CHG. Add a `deferred-tracked` lint that scans ADR prose for "deferred", "future CHG", "not yet implemented" and requires each to appear in `deferred.yaml`. Without this, the corpus accumulates forgotten obligations.

STRUCTFUT-PROC-017 — Phase files are unmoderated in count; PHASE-6+ has no structural slot. *Artifact:* ARCHITECTURE.md §11 table and `openspec/architecture/phases/`. *Pressure:* The architecture document lists only PHASE-0..5. REQ-ARCH-0002 actually pins the *existence* of exactly PHASE-0 through PHASE-5 as a phase-0 exit criterion. To add PHASE-6, the architecture must be re-gated. The structural mechanism for "new phase added" is undefined. *Transformation:* Add explicit prose in ARCHITECTURE.md §11 specifying how a new phase is allocated, plus relax REQ-ARCH-0002 to "PHASE-0..N exist, N ≥ 5" so adding PHASE-6 does not require a re-gate of PHASE-0. Reserve the structural rule now.

STRUCTFUT-PROC-018 — Per-project `openspec/` overlay scope is mentioned but its structural relationship to root `openspec/` is undefined. *Artifact:* ARCHITECTURE.md §3 (`projects/<project>/openspec/`), ADR-0001 §"Decision" ("project-scoped openspec overlay"), ADR-0002 §9. *Pressure:* No ADR defines how project specs *extend* or *shadow* root specs, how REQ-ID allocation handles project-vs-root namespacing, how cross-references between project and root resolve, or how the matrix unifies them. At PHASE-5 this works for one project; at 5+ projects, conflicting REQ-IDs, ambiguous xref resolution, and unclear shadow semantics become structural defects. *Transformation:* Add an ADR (ADR-0009 or amend ADR-0001) defining project-overlay semantics formally: shadow vs. extend, namespacing rule for REQ-IDs in project-scope (e.g., `REQ-<PROJECT>-<DOMAIN>-<NNNN>`), matrix unification rule. Land this *before* PHASE-5 so the first project sets a sound precedent.

---

**Summary.** Eighteen findings: 3 CRIT (001 directory partitioning, 002 amendment-log scaling, 003 supersession threshold), 7 SER (004 domain registry, 005 gate-table scaling, 006 CHG sharding, 007 xref graph, 008 parallel-vendoring, 009 methodology slot, 010 phase-stream branching), 8 PROC (011 duplicate prose, 012 ADR clusters, 013 ID padding, 014 staging sharding, 015 frontmatter fields, 016 deferred inventory, 017 phase-count flex, 018 project-overlay semantics). The structural dimension under most predictable pressure is **ADR-and-amendment surface area**: the directory layout, file granularity, frontmatter schema, amendment-log encoding, supersession threshold, and cluster-grouping artifacts all assume a 10-ADR corpus and break at 50+. Every other pressure (gate inventory, CHG sharding, xref graph) compounds this primary pressure because each new ADR amendment triggers gate additions, CHG creation, and cross-reference fan-in.
